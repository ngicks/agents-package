{
  "version": 1,
  "hooks": {
    "PostToolUse": [
      {
        "type": "command",
        "matcher": "edit|create",
        "bash": "if test -f go.mod && command -v goimports >/dev/null 2>&1; then goimports -w .; fi",
        "powershell": "if ((Test-Path go.mod) -and (Get-Command goimports -ErrorAction SilentlyContinue)) { goimports -w .; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }",
        "timeoutSec": 120
      }
    ]
  }
}
