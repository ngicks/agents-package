{
  "version": 1,
  "hooks": {
    "PostToolUse": [
      {
        "type": "command",
        "matcher": "edit|create",
        "bash": "if test -f go.mod && command -v golangci-lint >/dev/null 2>&1; then golangci-lint run; fi",
        "powershell": "if ((Test-Path go.mod) -and (Get-Command golangci-lint -ErrorAction SilentlyContinue)) { golangci-lint run; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE } }",
        "timeoutSec": 300
      }
    ]
  }
}
