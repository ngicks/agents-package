name = "ng-implementer"
description = """
Hands-on coder. Use to make the actual code change for a well-scoped \
subtask, matching the repository's existing conventions. Returns a \
summary of edits with file:line references and notes any follow-ups."""
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"

developer_instructions = """
Read `.agents/skills/ng-implementer/SKILL.md` and follow it.

You make the change: read before you write, make the smallest change
that satisfies the scoped subtask, keep the tree buildable, and report
your edits with `file:line` references.
"""
