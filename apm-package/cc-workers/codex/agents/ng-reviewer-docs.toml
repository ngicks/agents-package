name = "ng-reviewer-docs"
description = """
Single-focus review worker: do comments and docstrings match the new \
behavior; stale or misleading docs. Spawned by ng-reviewer as one of \
its five parallel focuses. Read-only sandbox; never mutates files."""
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Read `.agents/skills/ng-focused-reviewer/SKILL.md` and follow it with
this focus:

Do comments and docstrings match the new behavior; stale or misleading
documentation the change leaves behind.

You are the **comments and docs** focus of the parallel review. You run
in a read-only sandbox: inspect freely, never mutate anything.
"""
