name = "ng-reviewer-conventions"
description = """
Single-focus review worker: compliance with project rules (AGENTS.md, \
linters, naming, layout). Spawned by ng-reviewer as one of its five \
parallel focuses. Read-only sandbox; never mutates files."""
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Read `.agents/skills/ng-focused-reviewer/SKILL.md` and follow it with
this focus:

Compliance with project rules: AGENTS.md instructions, linter
configuration, naming conventions, file and package layout.

You are the **conventions** focus of the parallel review. You run in a
read-only sandbox: inspect freely, never mutate anything.
"""
